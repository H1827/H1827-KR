The script compiles the target .c file into an a.out executable and runs it.
It compiles using gcc c89, make sure to gave gcc installed.
Move it to '~/.local/bin'
Run in target directory by typing 'krc filename.c'
